link(rel="stylesheet", href="/styles/ofertas.css")
script(defer, src="/js/ofertas.js")

h2 Gestión de Ofertas Laborales
p Aquí podrás crear, editar y eliminar ofertas laborales para los roles de tu institución.
hr
section(style="display: flex; flex-direction: row; gap: 20px; margin-bottom: 10px; margin-top: 10px;")
    //- Filtros simples (opcional)
    .filtros
        label Estado:
        select#filtroEstado
            option(value="todas") Todas
            option(value="activas") Activas
            option(value="inactivas") Inactivas
    button#createOfertaBtn Crear nueva oferta
//- Lista de ofertas
table#tablaOfertas
    thead
        tr
            th ID
            th Rol
            th Descripción
            th Fecha Publicación
            th Fecha Cierre
            th Estado
            th Acciones
    tbody
        //- Aquí se llenarán las filas con JavaScript
div#modalCrearOferta.modal.hidden
    div.modal-content
        h3 Crear nueva oferta
        form#formCrearOferta
            label Rol requerido:
            .roles-radio
                label(for="rol_id") Médico
                    input(type="radio" name="rol_id" value="3" required)
                label(for="rol_id") Enfermero
                    input(type="radio" name="rol_id" value="4")
                label(for="rol_id") Recepcionista
                    input(type="radio" name="rol_id" value="5")
        
            label(for="descripcion") Descripción:
            textarea(name="descripcion" required)

            label(for="fecha_cierre") Fecha de cierre:
            input(type="date" name="fecha_cierre" required)

            button(type="submit") Crear oferta
            button(type="button" id="cerrarModal") Cancelar